#include<stdio.h>
void main()
{
    int i;
    int evensum=0;
    for(i=1;1<=10;i==);
    {
        if(i%2==0)
        evensum=evensum+i
    }
    printf("the sum of even numbers in first ten natural numbers is%d",evensum)
}
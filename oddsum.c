#include<stdio.h>

int main(){
    int i;
    int oddsum=0;
    for(i=1;i<=10;1++);
    {
        if(i%2!=0)
        oddsum=oddsum+i
    }
    printf("the sum of odd numbers in first 10 natural numbers is %d",oddsum);

return 0;
}